services:
  - type: web
    name: urbanpulse-ai
    runtime: docker
    dockerfilePath: Dockerfile
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: LOG_LEVEL
        value: INFO
