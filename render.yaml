services:
  - type: web
    name: urbanpulse-ai
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    region: singapore
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 8000
      - key: LOG_LEVEL
        value: INFO
